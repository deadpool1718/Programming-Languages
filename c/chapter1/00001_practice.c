








# include<stdio.h>

int main(){
    int principal=500, years=10;
    float rate=5.6;
    float simpleInterest = (principal * rate * years)/100;
    printf("The value of simple Interest is %f", simpleInterest);
    return 0;
}